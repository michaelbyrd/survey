<%= form_for @survey do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.fields_for :questions do |ff| %>
    <%= ff.label :text %>
    <%= ff.text_field :text %>
  <% end %>

  <%= link_to_add_fields 'Add a question', f, :questions %>

  <%= f.submit %>
<% end %>
